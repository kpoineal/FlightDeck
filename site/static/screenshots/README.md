# Screenshots

This directory is populated at build time by copying from `docs/screenshots/`.

Run `npm run copy:screenshots` before building the site:

```bash
cd site
npm run copy:screenshots
npm run build
```
