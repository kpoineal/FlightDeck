# Fact Checker — History

## Learnings

Initial scaffold via `squad upgrade`. Ready for work.

### 2026-09-04: FlightDeck 2.0 parity verification

- `47bb048` is the exact first-parent capability baseline for inbox merge `215fc1f`; `f2497e5` is a later feature-branch housekeeping commit with no `src/**` changes and byte-identical audited prompt surfaces.
- Verified current runtime defects: Add Item calls unbound `normalizeItem`; preload omits local `APP_RESUMED`; scanner deletion only removes hot items; proposal context ignores `lastChangedAt` and takes the oldest six entries from newest-first history; scanner `Inbound` normalizes to `unknown`; Today and KPI blocked counts read absent `isBlocked`; Inbox ignores the visible sort selection.
- Scanner/monitor prompt drift from `47bb048` is minimal: current changes add exact-source URL metadata plus a separate proposal-synthesis prompt, not a wholesale rewrite.
- Runtime-unreachable components: high confidence for `ScannerSection`/`TrackerCard`/`TrackerRow`/`EditableField`, `MailboxView`, `ComposeDraftModal`, `PromptEditor`, `HistoryItem`, `MorningBanner`, `HeatmapStrip`, `MomentumGauge`, and `PulseStrip`. Tests, docs, retained CSS, and persisted legacy UI preferences make deletion risk uneven; `MailboxView` removal and Inbox chronological sorting are explicit current test contracts.
- Validation: renderer build passed; focused parity suite passed 53/53; full suite passed 295 with 4 skipped. Those checks do not exercise the runtime-only Add Item or preload subscription failures.

### 2026-09-08: Inbox parity rollout verification

**Overall verdict:** Qualified green. No material shared claim was false, so no correction inbox entry was created. No product file or orphan candidate was edited or deleted by this verification.

**Claim ratings:**
- **Verified - Node suite:** Fresh `npm test` completed with 344 tests, 41 suites, 340 passed, 0 failed, 0 cancelled, 4 skipped, and exit 0.
- **Verified - renderer build:** Fresh `npm run build:renderer` transformed 195 modules and completed in 521 ms with exit 0. It emitted existing Svelte deprecation/accessibility diagnostics, so "build passed" is accurate but "warning-free" would not be.
- **Verified - browser acceptance and health:** After the npm wrapper failed to return diagnostics through the terminal transport, the already-built direct harness `node scripts/test-radar-browser.js` passed all 60 scenarios. Metrics were all zero: unexpected external requests, page errors, console errors, desktop/mobile overflow failures, and desktop/mobile overlap failures. Sixteen screenshots were emitted by the harness.
- **Verified - prompt golden and current protected-path drift:** `test/prompt-fidelity.test.js` passed 2/2 exact-string tests. Current worktree diff count is zero for `src/prompts/**`, `src/svelte/lib/prompts.js`, and `src/svelte/lib/constants.js`. Relative to `47bb048`, three paths differ: new `proposal-synthesis.md` plus `constants.js` and `prompts.js`; review evidence shows the latter changes are approved evidence-URL/schema additions rather than scanner/monitor instruction-semantic drift.
- **Qualified - performance improvement:** Fresh real-module profiling at 500 hot plus 9,500 cold items measured 14.83 ms median / 32.65 ms p95 for preview, 19.15/21.37 ms for reassign execute, and 19.63/20.65 ms for delete-all execute. Fifty same-tick saves with 10 ms injected write latency produced exactly one canonical write and all 50 waiters resolved true. Current performance and coalescing are verified; the magnitude of improvement is qualified because the pre-optimization implementation was uncommitted and only its recorded 479-656 ms benchmark remains available for comparison.
- **Verified - orphan runtime/bundle evidence:** A fresh transitive static-import closure from `src/svelte/main.js` (48 files) and `src/svelte/popout.js` (23 files) reached none of the 12 named candidates. Candidate-specific markers were absent from every generated production JavaScript asset. All 12 source files still exist and Git reports zero worktree deletions.
- **Qualified - raw source packaging:** `package.json` explicitly includes `src/**/*` and `dist-renderer/**/*`, so electron-builder is configured to package all 12 raw sources even though their executable component code is absent from Vite JavaScript. No packaged directory or ASAR was built and inspected in this pass, so actual archive contents remain a release-build check.
- **Verified - D1 through D4 implementation direction:** The approved directive uses Recent by default with independent filters; source and browser coverage confirm that behavior. Scanner deletion defaults to `Keep and reassign`, exposes explicit acknowledged `Delete all`, and uses preview-token transactions. Frequent scanner/item controls are mounted in Inbox while full monitoring prompt and detailed schedule/signal/notification controls remain in popout/advanced surfaces. D4 remained evidence-only: no orphan deletion occurred and every proposed deletion set remains unapproved.

**Devil's advocate:**
- Strongest 30-day failure scenario: a real long-lived v1.3 store contains cold items, effectful action receipts, legacy compatibility fields, and stored prompts that are not represented by deterministic browser fixtures. A user deletes or reassigns a scanner while an autosave, scanner run, or popout update is active; the UI reports success, but restart reveals lost audit evidence, stale assignments, or regenerated prompt/context. The current suite strongly covers synthetic transaction and browser paths but does not replace a byte-faithful prior-release migration/restart test against a packaged Electron build.
- Load-bearing assumptions: the browser bridge matches Electron IPC/electron-store receipt and timing behavior; all renderer loading remains statically discoverable without computed runtime imports; generated `dist-renderer` corresponds to the exact worktree under review; `src/**/*` packaging behaves as configured; 500 hot/9,500 cold with sparse proposal/history data represents large real stores; and prompt/schema additions remain intentionally approved metadata rather than hidden semantic changes.
- Safest first manual smoke sequence: back up the real FlightDeck store; launch the packaged app without reseeding; confirm existing Inbox order, filters, stored monitor prompt, and archived hydration; create an item under a non-default scanner and restart; edit scanner/owner/due/done fields rapidly and restart; run one item and one scanner; delete a disposable scanner first with Keep and reassign and restart; create another disposable scanner with hot and archived items, inspect the impact counts, use Delete all, and restart; verify action evidence/history and tombstones; finally test popout prompt editing plus app focus/resume refresh. Stop on any count, prompt, receipt, assignment, or post-restart mismatch.
