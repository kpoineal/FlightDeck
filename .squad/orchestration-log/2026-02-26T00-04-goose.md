# Orchestration Log — Goose (Links & Due Date Fixes)

**Timestamp:** 2026-02-26T00:04:00Z  
**Agent:** Goose (Frontend Dev, claude-sonnet-4.5)  
**Mode:** Background  
**Outcome:** SUCCESS

## Task

Fix broken link handling and due date bugs reported by user.

## Changes

1. **Relaxed `isDeepLink()`** — replaced rigid ~12-regex whitelist with a permissive gate: any HTTPS URL with a non-trivial path (`pathname.length > 1`) is accepted. Known Microsoft domains still fast-path via `DEEP_LINK_PATTERNS`.
2. **Ledger evidence link parsing** — `applyLedgerPayload()` now handles `{label, type, url}` structured objects in `evidenceLinks` alongside raw URL strings. `collectRadarSourceLinks()` updated to match.
3. **Temporal inference in prompts** — `prompts/radar-scan.md` and `renderer/prompts.js` now instruct the LLM to resolve relative temporal language ("by end of week") to concrete ISO-8601 `dueAt` dates using today's date as reference.
4. **Evidence link guidance** — `prompts/radar-scan.md` now includes explicit rules for populating `evidenceLinks` with citation URLs, descriptive labels, types, and prohibits fabricated URLs.

## Coordinator Fix

After Merlin's tests caught a bug, the Coordinator fixed the HTTP protocol check in `isDeepLink()` to properly require HTTPS.

## Files Modified

- `renderer/utils.js`
- `renderer/models/radar.js`
- `renderer/prompts.js`
- `prompts/radar-scan.md`
