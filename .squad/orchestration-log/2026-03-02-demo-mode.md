# Orchestration Log — 2026-03-02 Demo Mode

## Agents

| Agent | Role | Scope |
|-------|------|-------|
| Maverick | Strategy analysis | Evaluated 4 demo options, identified DEC-010 ephemeral gap, recommended built-in demo mode (Option 4) |
| Viper | Fixture + interceptor build | Created `src/demo/fixture.json` with synthetic data, built `src/renderer/demo.js` interceptor, wired `--demo` CLI flag in `src/main/index.js` |
| Goose | HTML wiring | Updated `src/index.html` with demo.js script tag, adjusted `src/renderer/app.js` init flow, added demo-related styles to `src/styles/layout.css` |

## Files Modified

| File | Status | Agent |
|------|--------|-------|
| `src/demo/fixture.json` | New | Viper |
| `src/renderer/demo.js` | New | Viper |
| `src/renderer/app.js` | Modified | Goose |
| `src/main/index.js` | Modified | Viper |
| `src/index.html` | Modified | Goose |
| `src/styles/layout.css` | Modified | Goose |
| `package.json` | Modified | Viper |

## Sequence

1. **Maverick** analyzed 4 demo data strategies; identified that radar items are ephemeral (DEC-010) so only a `runWorkiqJson()` intercept can populate the Radar tab offline. Recommended Option 4 (built-in demo mode).
2. **Viper** built `fixture.json` with synthetic persisted state + canned WorkIQ responses, implemented `demo.js` interceptor (monkey-patches `runWorkiqJson()` based on prompt keyword sniffing), added `--demo` CLI flag.
3. **Goose** wired `demo.js` into `index.html` script loading order (before `app.js`), updated `app.js` init to call demo seeding, added layout styles for demo indicator.

## Outcome

Full demo mode implemented. App launches with `--demo` flag or `?demo=1` query param, loads synthetic fixture data, intercepts all WorkIQ calls with canned responses. Zero M365 dependency, perfect reproducibility, no production code paths modified.
