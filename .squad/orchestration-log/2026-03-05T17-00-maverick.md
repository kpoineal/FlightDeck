# Orchestration Log — 2026-03-05 Maverick (Inline Citation Extraction — Architecture)

**Agent:** Maverick  
**Role:** Lead — architecture analysis and approach recommendation  
**Session:** Inline citation extraction from AI summary/reason fields

## Completed Work

- Analyzed three candidate approaches for extracting evidence links from inline markdown citations in AI-generated summary/reason fields: Replace (drop structured array), Supplement (extract inline + keep structured array), Keep current (structured only).
- Recommended **SUPPLEMENT** approach — extract inline citations from raw text before `cleanDisplayText()` strips them, merge with structured `evidenceLinks` array, deduplicate by normalized URL.
- Defined data flow order-of-operations: extract inline → normalize structured → merge/dedup → clean display text.
- Specified `extractInlineCitations()` function design reusing existing validators (`normalizeExternalUrl`, `isHallucinatedUrl`, `isGenericUrl`, `isDeepLink`, `inferSignalTypeFromUrl`).
- Identified integration points: `applyRadarPayload()` (primary), `monitorTaskItem()` (secondary), `applyLedgerPayload()` (tertiary/deferred).
- Recommended prompt relaxation: remove prohibition on inline citations, keep structured `evidenceLinks` as preferred source.
- Delegated implementation to Goose, tests to Merlin, parsing safety analysis to Viper.

## Decision Written

- Architecture decision written to `decisions/inbox/maverick-link-extraction-arch.md`.

## Handoff Context

- Goose received implementation scope: `utils.js`, `models/radar.js`, `models/tracking.js`, `monitor-engine.js`, `constants.js`.
- Merlin received test scope: `extractInlineCitations()` unit tests + `applyRadarPayload()` integration tests.
- Viper confirmed JSON parsing safety and prompt strategy alignment.
