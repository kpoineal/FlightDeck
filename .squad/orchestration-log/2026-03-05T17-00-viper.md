# Orchestration Log — 2026-03-05 Viper (Inline Citation Extraction — Parsing Analysis)

**Agent:** Viper  
**Role:** Backend Dev — prompt and JSON parsing analysis  
**Session:** Inline citation extraction from AI summary/reason fields

## Completed Work

- Audited current prompt strategy across all 4 prompt sources (radar-scan.md, briefing.md, day-briefing.md, monitoring prompt in prompts.js) for inline citation handling.
- Confirmed markdown citation characters (`[`, `]`, `(`, `)`, `%`, `&`, `=`) are all legal in JSON strings — no parsing risk from inline citations.
- Verified `sanitizeLikelyBrokenJson()` character-by-character quote tracking is unaffected by URL content in JSON string values.
- Verified `normalizeJsonCandidate()` operations (curly quote replacement, ANSI strip, trailing comma removal) do not damage URLs.
- Verified `parseJsonWithRepair()` newline collapse handles long SharePoint URLs split across lines.
- Mapped the extraction gap: inline citations inside JSON string values not covered by `extractFootnoteCitations()` (outside JSON only) or `extractExternalUrls()` (returns URLs without labels).
- Designed `extractMarkdownCitations(text)` function spec with regex and `citationsToEvidenceLinks()` conversion helper.
- Recommended three-layer citation defense: structured `evidenceLinks` (primary), inline extraction (new supplement), footnote injection (existing fallback).
- Confirmed structured `evidenceLinks` should remain primary — provides typed metadata and `signalAt` timestamps not available from inline text.

## Decision Written

- `decisions/inbox/viper-link-extraction-parsing.md` (parsing analysis + recommendation)

## Handoff Context

- Confirmed to Maverick that inline citations are safe for JSON parsing.
- Aligned with Maverick on SUPPLEMENT approach — no backend changes required, purely renderer-side.
